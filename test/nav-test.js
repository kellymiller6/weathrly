import Nav from '../lib/components/Nav';
import React from 'react';
import { shallow } from 'enzyme';

describe('Nav', () => {
  it('should change the state based on user input', () => {
    const nav = shallow(<Nav />)
    // const input = nav.find('')
  });







});
